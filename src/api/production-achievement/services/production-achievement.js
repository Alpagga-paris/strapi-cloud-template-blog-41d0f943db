'use strict';

/**
 * production-achievement service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::production-achievement.production-achievement');
