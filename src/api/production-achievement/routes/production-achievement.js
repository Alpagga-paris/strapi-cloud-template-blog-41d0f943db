'use strict';

/**
 * production-achievement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::production-achievement.production-achievement');
