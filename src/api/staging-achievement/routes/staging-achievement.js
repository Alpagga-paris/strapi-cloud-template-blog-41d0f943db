'use strict';

/**
 * staging-achievement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::staging-achievement.staging-achievement');
