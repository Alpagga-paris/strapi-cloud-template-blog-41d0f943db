'use strict';

/**
 * staging-display-destockage router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::staging-display-destockage.staging-display-destockage');
